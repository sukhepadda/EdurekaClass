import React from "react";

const HOME = () => (
  //we are using round because there is only 1 return value like this a = (bently) => bently;
  <div>
    <h1 className="mt-4">Welcome to The Homepage of THE REACT practice folder...</h1>
  </div>
);

export default HOME;
