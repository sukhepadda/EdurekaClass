import React from "react";

const ThirddComponents = () => (
  //we are using round because there is only 1 return value like this a = (bently) => bently;
  <div>
    <p>Third Component</p>
  </div>
);

export default ThirddComponents;
